<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CareMate" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/caremate_primary</item>
        <item name="colorSecondary">@color/caremate_secondary</item>
        <item name="colorTertiary">@color/caremate_tertiary</item>
        <item name="colorSurface">@color/caremate_surface</item>
        <item name="colorOnSurface">@color/caremate_neutral</item>
        <item name="colorOutline">@color/caremate_outline</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.CareMate" parent="Base.Theme.CareMate" />

    <!-- Splash Screen Theme -->
    <style name="Theme.CareMate.MySplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/caremate_primary</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_round</item>
        <item name="postSplashScreenTheme">@style/Theme.CareMate</item>
    </style>
</resources>